name: flutter_disaster_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dart_nostr: ^9.2.5
  shared_preferences: ^2.2.3
  http: ^1.2.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  sqlite3: ^2.4.7
  provider: ^6.1.5+1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true